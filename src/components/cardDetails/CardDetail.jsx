const CardDetail = ({ card }) => {
  const { img, card_bg, category, category_bg, title, txt_btn_bg, id } = card;
  console.log(card);
  return <div>you clliked me</div>;
};

export default CardDetail;
